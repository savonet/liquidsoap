sudo: required

services:
  - docker

before_install:  
  - sudo apt-get update
  - sudo yes | apt-get install --yes docker-engine

script:
  - docker build --no-cache --build-arg TRAVIS_COMMIT="$TRAVIS_COMMIT" -f .travis.docker-file .
