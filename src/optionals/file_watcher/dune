(library
 (name liquidsoap_file_watcher)
 (libraries
  liquidsoap-lang
  liquidsoap_core
  unix
  (select
   file_watcher.ml
   from
   (inotify -> file_watcher.inotify.ml)
   (-> file_watcher.mtime.ml)))
 (library_flags -linkall)
 (wrapped false))
