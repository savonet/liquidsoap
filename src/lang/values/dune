; Runtime values, the global environment they are registered in, and the
; on-disk caches keyed on them.

(generate_sites_module
 (module sites)
 (sites liquidsoap-lang))

(library
 (name liquidsoap_lang_values)
 (public_name liquidsoap-lang.values)
 (wrapped false)
 (flags
  (:standard -open Liquidsoap_lang_prelude -open Liquidsoap_lang_data))
 (preprocess
  (pps ppx_hash))
 (libraries
  liquidsoap-lang.ast
  liquidsoap-lang.data
  liquidsoap-lang.prelude
  liquidsoap-lang.types
  dune-site
  unix))
