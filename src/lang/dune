(env
 (dev
  (flags
   (:standard -w -7-9 -warn-error -33))))

(include_subdirs unqualified)

(menhir
 (modules parser json_parser))

(library
 (name liquidsoap_lang)
 (public_name liquidsoap-lang)
 (preprocess
  (pps sedlex.ppx))
 (wrapped false)
 (library_flags -linkall)
 (libraries
   menhirLib
   (select lang_regexp.ml from (pcre -> lang_regexp.pcre.ml) (-> lang_regexp.str.ml)))
 (modules
  console
  doc
  documentation
  environment
  error
  evaluation
  json_base
  json_lexer
  json_parser
  json
  lang_content
  lang_core
  lang_error
  lang_frame
  lang_hooks
  lang_regexp
  lang_string
  lexer
  modules
  parser
  parser_helper
  plug
  pos
  preprocessor
  profiler
  repr
  runtime
  runtime_error
  term
  type
  typechecking
  typing
  unifier
  value))
