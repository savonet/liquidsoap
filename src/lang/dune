(env
 (dev
  (flags
   (:standard -w -7-9 -warn-error -33))))

(include_subdirs unqualified)

(menhir
 (modules parser json_parser))

(library
 (name liquidsoap_lang)
 (preprocess
  (pps sedlex.ppx))
 (wrapped false)
 (library_flags -linkall)
 (libraries menhirLib pcre)
 (modules_without_implementation frame)
 (modules
  console
  doc
  documentation
  environment
  error
  evaluation
  frame
  json_base
  json_lexer
  json_parser
  json
  lang
  lang_content
  lang_core
  lang_error
  lang_string
  lexer
  modules
  parser
  parser_helper
  plug
  pos
  preprocessor
  profiler
  repr
  runtime
  runtime_error
  term
  type
  typechecking
  typing
  unifier
  value))
