(env
 (release
  (ocamlopt_flags
   (:standard -O2)))
 (dev
  (flags
   (:standard -w -9))))

(library
 (name liquidsoap_tooling)
 (public_name liquidsoap-lang.tooling)
 (flags
  (:standard -open Liquidsoap_lang_prelude))
 (libraries sedlex liquidsoap-lang.prelude liquidsoap_lang))
