(executables
 (names http telnet)
 (libraries duppy re unix threads.posix))

; Nothing else builds the examples, so this is what keeps them compiling.

(rule
 (alias citest)
 (deps ./http.exe ./telnet.exe)
 (action (progn)))
