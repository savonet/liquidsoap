(executables
 (names detect check)
 (libraries dune-configurator unix))

(rule
 (targets ao_available ao_c_flags.sexp ao_c_library_flags.sexp)
 (action
  (run ./detect.exe --context %{context_name})))
