(executable
 (name gen)
 (modules gen has_mad)
 (libraries
  (select
   has_mad.ml
   from
   (mad -> has_mad.yes.ml)
   (-> has_mad.no.ml))))

(rule
 (target examples.inc)
 (action
  (with-stdout-to
   examples.inc
   (run ./gen.exe))))
