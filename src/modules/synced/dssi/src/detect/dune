(executables
 (names detect check)
 (libraries dune-configurator unix))

(rule
 (targets dssi_available dssi_c_flags.sexp dssi_c_library_flags.sexp)
 (action
  (run ./detect.exe --context %{context_name})))
