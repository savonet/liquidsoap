(env
 (release
  (ocamlopt_flags
   (:standard -O2)))
 (dev
  (flags
   (:standard -w -9))))

(menhir
 (modules parser))

(library
 (name cron)
 (libraries menhirLib unix)
 (preprocess
  (pps sedlex.ppx))
 (synopsis "Cron utilities"))
