(library
 (name liquidsoap_prometheus)
 (flags
  (:standard -open Liquidsoap_core))
 (libraries
  cohttp
  cohttp-lwt
  cohttp-lwt-unix
  dtools
  http
  liquidsoap_core
  lwt
  prometheus
  prometheus-app
  threads.posix
  unix)
 (wrapped false)
 (optional))
