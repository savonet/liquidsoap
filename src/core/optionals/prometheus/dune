(library
 (name liquidsoap_prometheus)
 (flags
  (:standard -open Liquidsoap_core))
 (libraries
  cohttp-lwt-unix
  dtools
  liquidsoap_core
  lwt
  prometheus
  prometheus-app
  threads.posix)
 (wrapped false)
 (optional))
