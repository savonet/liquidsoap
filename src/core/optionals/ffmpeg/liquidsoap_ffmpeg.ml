(* Empty on purpose: this module exists so that the aggregating
   `liquidsoap_ffmpeg` library has something to compile. The ffmpeg support
   itself lives in the `base`, `decoder`, `encoder`, `filter` and `io`
   sub-libraries, which this one depends on. *)
