(install
 (section
  (site
   (liquidsoap-lang libs)))
 (package liquidsoap)
 (files
  (glob_files *.liq)))

(install
 (section
  (site
   (liquidsoap-lang libs)))
 (package liquidsoap)
 (files
  (glob_files extra/*.liq)))
