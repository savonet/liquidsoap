(install
 (section
  (site
   (liquidsoap-lang libs)))
 (package liquidsoap-libs)
 (files
  (glob_files *.liq)))

(install
 (section
  (site
   (liquidsoap-lang libs)))
 (package liquidsoap-libs-extra)
 (files
  (glob_files extra/*.liq)))
