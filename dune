(rule
 (targets liquidsoap.config)
 (alias base)
 (mode
  (promote (until-clean)))
 (action
  (run ./src/bin/build_config.exe)))

(alias
 (name default)
 (deps
  (alias_rec base)))

(alias
 (name install)
 (deps
  (alias_rec base)))
